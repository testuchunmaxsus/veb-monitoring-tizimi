# Core
Django==5.0.9
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1

# CORS, filters, throttling
django-cors-headers==4.4.0
django-filter==24.3
django-ratelimit==4.1.0

# Real-time (WebSocket)
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2

# OpenAPI / Swagger
drf-spectacular==0.27.2

# Background tasks (SQLite-friendly)
django-q2==1.7.4

# Geolocation (offline)
geoip2==4.8.0

# User-Agent parsing
user-agents==2.2.0

# PDF generation
reportlab==4.2.5

# Image (required by Pillow-dependent libs)
Pillow==10.4.0

# Environment loading
python-decouple==3.8

# Production server
gunicorn==22.0.0
whitenoise==6.7.0

# PostgreSQL (Railway uchun)
psycopg2-binary==2.9.9

# PostgreSQL (optional - SQLite uchun kerak emas)
# Agar PostgreSQL ishlatmoqchi bo'lsangiz, uncomment qiling:
# psycopg2-binary==2.9.9

# Email backend (already in Django, but explicit)
# Celery alternative not used; django-q2 is enough

# Testing
pytest==8.3.3
pytest-django==4.9.0
pytest-asyncio==0.24.0
factory-boy==3.3.1
faker==30.3.0

# Dev tools
django-debug-toolbar==4.4.6
ipython==8.27.0
